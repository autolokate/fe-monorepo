/**
 * Install UI is rendered by {@link PwaInstallProvider} (fixed bottom dock + iOS sheet).
 * This module remains for backwards-compatible imports from docs and barrel exports.
 */
export function PwaInstallPrompt() {
  return null;
}
