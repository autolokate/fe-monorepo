export interface HeroCopy {
  eyebrow: string;
  headline: string;
  headlineAccent: string;
  description: string;
  note: string;
  primaryCta: {
    label: string;
    href: string;
  };
  secondaryCta: {
    label: string;
    href: string;
  };
}
