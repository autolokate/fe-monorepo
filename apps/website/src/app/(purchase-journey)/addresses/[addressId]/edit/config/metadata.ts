import type { Metadata } from 'next';

export const editAddressMetadata: Metadata = {
  title: 'Edit address — Autolokate',
  description: 'Edit a saved delivery address.',
  robots: { index: false, follow: false },
};
