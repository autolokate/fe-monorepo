import type { Metadata } from 'next';

export const newAddressMetadata: Metadata = {
  title: 'Add address — Autolokate',
  description: 'Add a new delivery address.',
  robots: { index: false, follow: false },
};
