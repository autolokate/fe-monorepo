import type { Metadata } from 'next';

export const addressesMetadata: Metadata = {
  title: 'My addresses — Autolokate',
  description: 'Manage where your Autolokate orders are delivered.',
  robots: { index: false, follow: false },
};
