export interface WhyItMattersCopy {
  eyebrow: string;
  headline: string;
  headlineAccent: string;
  stat: {
    value: string;
    label: string;
    source: string;
  };
  body: string;
  callout: string;
  map: {
    heading: string;
    pill: string;
  };
}
