export interface Testimonial {
  id: string;
  quote: string;
  name: string;
  role: string;
}

export interface TestimonialsCopy {
  eyebrow: string;
  headline: string;
  headlineAccent: string;
}
