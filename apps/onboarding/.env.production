# Baked into Vite production builds (e.g. Vercel) when dashboard env vars are unset.
# Override in Vercel → Settings → Environment Variables when the backend URL changes.
#
# Consumer onboarding API (auth, QR resolve, legal, checkout) — NOT apps/website staging.

VITE_API_BASE_URL=https://malisa-noninclusive-davin.ngrok-free.dev
VITE_ENVIRONMENT=development
VITE_ENABLE_LOGS=false

# Razorpay test publishable key (key_id only — for checkout testing on Vercel)
VITE_RAZORPAY_KEY=rzp_test_T7vT7zqV13HcS2
